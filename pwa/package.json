{
  "name": "pingolin-pwa",
  "version": "3.0.20",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "watch:elm": "chokidar 'src/**/*.elm' -c 'elm make src/Main.elm --output=public/main.js' --initial",
    "watch:ts": "tsc -p tsconfig.json --watch",
    "predev": "node generate-version.js",
    "dev": "concurrently -n \"SERVER,ELM,TS\" -c \"cyan,magenta,yellow\" \"node server.js\" \"npm run watch:elm\" \"npm run watch:ts\"",
    "prebuild": "node generate-version.js",
    "build:ts": "tsc -p tsconfig.json",
    "build:elm": "elm make src/Main.elm --output=public/main.js --optimize && mkdir -p dist && cp -rv public/* dist/ && touch dist/.nojekyll",
    "build": "npm run build:ts && npm run build:elm",
    "test:e2e": "playwright test tests/fortress.spec.ts",
    "test:unit": "vitest run tests/sync-worker.spec.js",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "chokidar-cli": "^3.0.0",
    "concurrently": "^10.0.3",
    "elm": "^0.19.1-6",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "^3.46.0-build1"
  }
}
